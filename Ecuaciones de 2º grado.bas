1 REM ECUACIONES DE SEGUNDO GRADO
2 GOTO 20
3 PRINT
PRINT
PRINT
5 PRINT "DA VALORES PARA A, B Y C ": INPUT A, B, C
6 IF A$ = "EXIT" THEN GOTO 141
10 GOTO 30
20 PRINT "DA VALORES PARA A, B Y C (SEPARADOS POR COMAS)": INPUT A, B, C
30 D = B ^ 2 - 4 * A * C
40 IF D < 0 THEN GOTO 90
42 IF D = 0 THEN GOTO 75
50 X1 = (-B + D ^ (1 / 2)) / (2 * A)
60 X2 = (-B - D ^ (1 / 2)) / (2 * A)
70 PRINT "LAS RAICES SON:"; X1; "Y "; X2
72 GOTO 110
75 PRINT "LA UNICA RAIZ ES: "; X1
80 GOTO 110
90 PRINT "LA ECUUACION NO TIENE RAICES REALES"
110 PRINT "`TIPEA` 'E' PARA SALIR Y 'C' PARA CONTINUAR"
120 INPUT A$
130 IF A$ = "E" THEN GOTO 141
135 IF A$ = "C" THEN GOTO 3
140 IF A$ <> "E" THEN GOTO 110
141 PRINT
145 CLS : REM "Borrar la pantalla"
150 END


